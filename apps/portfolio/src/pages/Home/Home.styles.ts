import styled from 'styled-components'
import { flex } from '@portfolio/utils'

export const StyledHomeWrapper = styled.div`
  width: 100%;
  height: 100%;
  ${flex('flex', 'row', 'center', 'center')};
`
