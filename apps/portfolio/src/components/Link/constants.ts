import { LinkProps } from './interfaces'

export const COLORS: { [key: string]: LinkProps['color'] } = {
  default: 'default',
  primary: 'primary',
  secondary: 'secondary',
}

export const SIZES: { [key: string]: LinkProps['size'] } = {
  default: 'default',
  big: 'big',
}
